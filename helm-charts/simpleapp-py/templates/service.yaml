apiVersion: v1
kind: Service
metadata:
  name: {{ include "simpleapp-py.fullname" . }}
  labels:
    {{- include "simpleapp-py.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "simpleapp-py.selectorLabels" . | nindent 4 }}
